<div class="pageContent" style="background-color:black; width:500px; height:500px;">hej</div>
<!-- <?php
    $name = $_POST['name']; //information för sändare, mottagare, ämne etc.
    $email = $_POST['mail'];
	$subject =   $_POST['subject'];
    $message = $_POST['message'];
    $from = 'From: Kontaktformuär';
    $to = 'claude.bennedahl@gmail.com';     
    $body = "From: $name\n E-Mail: $email\n Message:\n $message";	
	if ($_POST['submit']) {
		if ($name != '' && $email != '') { //skickar mail om informationen stämmer, och du trycker submit			 
				if (mail ($to, $subject, $body, $from)) { 
				$messagetitle = 'Success!'
				$message = '<p>Ditt meddelande har sänts!</p>';
			} else { 
				$messagetitle = 'Fel!';
				$message = '<p>Något gick fel. Försök igen!</p>'; 
			}
		} else {
			$messagetitle = 'Fel!';
			$message = '<p>Var god fyll i alla fält.</p>';
		}
	}
?> -->

